{
	"name": "uzumibi-on-cloudflare-spike",
	"version": "0.0.0",
	"private": true,
	"uzumibi": {
		"httpMaxBytes": 65536
	},
	"scripts": {
		"deploy": "pnpm run build:wasm:asyncify && wrangler deploy",
		"dev": "pnpm run build:wasm:asyncify && wrangler dev",
		"dev:vanilla": "pnpm run build:wasm:vanilla && wrangler dev --config wrangler.vanilla.jsonc",
		"build:wasm:vanilla": "node scripts/build-wasm.mjs vanilla",
		"build:wasm:asyncify": "node scripts/build-wasm.mjs asyncify",
		"build:wasm:queue": "node scripts/build-wasm.mjs queue",
		"dev:queue": "pnpm run build:wasm:queue && wrangler dev --config wrangler.queue.jsonc",
		"deploy:queue": "pnpm run build:wasm:queue && wrangler deploy --config wrangler.queue.jsonc",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"asyncify-wasm": "^1.2.0",
		"vitest": "~3.2.0",
		"wrangler": "^4.54.0"
	}
}
