{
	"name": "$$PROJECT_NAME$$",
	"version": "0.0.0",
	"private": true,
	"uzumibi": {
		"httpMaxBytes": 65536
	},
	"scripts": {
		"deploy": "pnpm run build:wasm:vanilla && wrangler deploy",
		"dev": "pnpm run build:wasm:vanilla && wrangler dev",
		"build:wasm:vanilla": "node scripts/build-wasm.mjs vanilla",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"vitest": "~3.2.0",
		"wrangler": "^4.54.0"
	}
}
